# Local dev broker for the MQTT exporter and the Home Assistant discovery path.
# Anonymous access is fine here and ONLY here — this listener is never exposed beyond
# the compose network, and the production exporter always authenticates.

listener 1883
allow_anonymous true
persistence false
log_dest stdout
